git clone https://github.com/Geovalga/Chatbot.git
cd Chatbot/
npm install
cd _nodejs/
npm install
cd ..
npm run build
"se pindah itu folder 'build' ke dalam folder '_nodejs'"
npm run back
"buka localhost:4000 di browser"
